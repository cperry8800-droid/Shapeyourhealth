# Copy this file to `.env` (same folder) and fill in the values.
# Get both from: Supabase dashboard → Project Settings → API
#
# SUPABASE_URL is the "Project URL" — safe to commit, but we keep it here
# alongside the secret for convenience.
#
# SUPABASE_SERVICE_ROLE_KEY is the "service_role" key. NEVER commit this,
# never paste it in chat, never put it in client-side code. It bypasses RLS.

SUPABASE_URL=https://zznufekgjngecelwxndw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-key-here
